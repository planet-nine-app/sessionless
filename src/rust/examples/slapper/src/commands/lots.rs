use crate::utils::Color;

pub fn lots(_color: Option<Color>, _language: Option<String>, _iterations: Option<u32>) -> anyhow::Result<()> {
    // TODO: Implement this. Remove the _'s from args above when you do
    warn!("Command 'lots' is not implemented!");
    Ok(())
}
